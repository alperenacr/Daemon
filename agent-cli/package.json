{
  "name": "daemon-agent",
  "version": "1.0.0",
  "description": "Daemon local AI agent — polls tasks, completes them, farms IDLE when idle",
  "main": "agent-node.js",
  "scripts": {
    "start": "node agent-node.js"
  },
  "dependencies": {
    "ethers": "^6.13.0",
    "dotenv": "^16.4.0"
  }
}
